# 后端 API 地址（开发环境通过 vite proxy 代理，生产环境需配置为实际后端地址）
# 开发环境：留空即可，vite.config.js 中 proxy 会转发 /api 到 http://localhost:3000
# 生产环境：配置为实际后端地址，如 https://api.your-domain.com
VITE_API_TARGET=http://localhost:3000

# 是否在请求头中显示调试信息
VITE_DEBUG=false
